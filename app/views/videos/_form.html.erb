<% form_tag(upload_path,:method=>'post', :multipart => true,:class=>'data-form') do  %>
  <% if @video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@video.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
      <% @video.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class="field">
  Name: <%= text_field_tag "name" %>
  </div>
 <div class="field">
  Description: <%= text_area_tag "description" %>
  </div>
  <div class="field">
 <%= file_field_tag :asset %>
  </div>
  <div class="actions">
    <%= submit_tag %>
  </div>
<% end %>
